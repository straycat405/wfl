build.gradle 전문

//build.gradle

plugins {
	id 'java'
	id 'org.springframework.boot' version '3.2.4'
	id 'io.spring.dependency-management' version '1.1.4'
}

group = 'com.example'
version = '0.0.1-SNAPSHOT'

java {
	sourceCompatibility = '20'
}

configurations {
	compileOnly {
		extendsFrom annotationProcessor
	}
}

repositories {
	mavenCentral()
}

dependencies {
	//JDBC
	implementation 'org.springframework.boot:spring-boot-starter-data-jdbc'
	//Spring-Boot
	implementation 'org.springframework.boot:spring-boot-starter-web'
	//Mybatis 3.0.3
	implementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter:3.0.3'
	//Lombok
	compileOnly 'org.projectlombok:lombok'
	//SpringBootDevtools
	developmentOnly 'org.springframework.boot:spring-boot-devtools'
	//MariaDB driver
	runtimeOnly 'org.mariadb.jdbc:mariadb-java-client'
	//Lombok Annotation
	annotationProcessor 'org.projectlombok:lombok'
	//test SB
	testImplementation 'org.springframework.boot:spring-boot-starter-test'
	//test Mybatis
	testImplementation 'org.mybatis.spring.boot:mybatis-spring-boot-starter-test:3.0.3'
	//ojdbc8
	runtimeOnly 'com.oracle.database.jdbc:ojdbc8'
	//mySQL connector
	runtimeOnly 'mysql:mysql-connector-java:8.0.25'
	//excel
	implementation group: 'org.apache.poi', name: 'poi', version: '4.1.2'
    implementation group: 'org.apache.poi', name: 'poi-ooxml', version: '4.1.2'
	//Gson (List를 json으로)
	implementation 'com.google.code.gson:gson:2.10.1'
	//H2 DB
	implementation 'com.h2database:h2'
	
}

tasks.named('test') {
	useJUnitPlatform()
}


/////////////////

1. 자바는 20버전 기준으로 했습니다

2. 서버는 Mybatis + H2db 사용

혹시 gitignore 걸릴까봐 application.properties 설정 

//application.properties

spring.application.name=wfltest-1 (프로젝트명)

## MariaDB (처음에 쓴거라 주석으로 꺼둠)
#spring.datasource.driver=org.mariadb.jdbc.Driver
#spring.datasource.url=jdbc:mariadb://127.0.0.1:3306/wfl_test
#spring.datasource.username=root
#spring.datasource.password=1234

#H2 DB (현재 쓰는 db)
spring.datasource.driver=org.h2.Driver
spring.datasource.url=jdbc:h2:tcp://localhost/~/wfltest (사용할 저장소 경로)
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# Mybatis config
mybatis.type-aliases-package=com.wfl.mbtest.user; (aliase 선언용이라 프로젝트 이름에 맞게)
mybatis.mapper-locations:classpath:mapper.xml (맵퍼 경로 맟출것)
mybatis.configuration.map-underscore-to-camel-case=true
logging.level.hello.itemservice.repository.mybatis=trace