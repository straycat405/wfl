<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wfl.mbtest.ledger.SpendingMapper">

	<select id="getSpending" parameterType="Integer" resultType="spending">
        SELECT *, (TO_CHAR(SPENDINGTIME,'YYYY-MM-DD HH:MM:SS')) as spendTime FROM SPENDING WHERE userId = #{userId}
	</select>
	
</mapper>