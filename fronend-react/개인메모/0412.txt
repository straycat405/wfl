자산관리

로그인 ->

로그인 이전에는 일단 

로그인 후 -> 가계부 선택 (게시판 형식)
	- 가계부 번호, 가계부 이름, 생성날짜, 수정날짜

가계부 선택 후 -> 가계부 페이지

	- 가계부 입/출력 기본화면
	- 메뉴이동 (가계부 조회)
	- 
00000000000


@아이디찾기

이름 + 연락처 (회원가입시 사용한 것으로)

-> 포스팅
	-> 백에서 조회
		-> 결과 레코드 있으면 반환 없으면 없다고 반환
						-> 프론트에서 결과 받은거 있으면 이메일 보여주고 로그인 (비밀번호 찾기 페이지 연결)
									-> 없으면 없습니다 끝


@ 비밀번호찾기

이메일 입력
	-> 포스팅 -> 백에서 조회 -> 결과 레코드 있으면 ok반환 없으면 no
	-> ok 받으면 해당이메일로 임시비밀번호 발급 (회원정보수정에서 고치라고 유도)


////////////

회원정보수정
이메일 (중복확인 후 수정가능) / 이름 / 닉네임 / 프로필 사진 지정 / 연락처 수정 가능

수정 후 moddate 최신화

put 전송 방식으로 수정

수정 후 localstorage의 유저정보도 최신화

관리자페이지

관리자로그인 만들기 + 관리자로그인 성공

관리자메뉴구성

- 유저정보 : 유저정보 테이블 (검색 , 번호조회)
- 유저삭제
- 유저정보 편집
- 관리자권한, 탈퇴설정 등

0403

// 회원가입시 랜덤닉네임 부여 기능 추가
// AccountSetting 추가 (계정 정보 수정)

** 테스트 끝나면 최종때는 main.jsx에서 <React.StrictMode> 지워주기

// 회원정보수정 프로필사진까지 했음

// 백쪽 쿼리 정상작동 수정 + 수정된값으로 받아오기 + navbar에 프로필 표시까지 수정

///////////////// 0405 금 //////////////////

관리자로그인 -> 유저 정보

테이블 리턴 + api로 유저데이타 받기

테이블 뿌리기

0408 //

테이블 꾸미기 + 유저삭제버튼

검색 + 유저정보수정

//
목표

1. 페이지 이동시 전체리스트로 조회 발동 

2. 검색기능 사용시 검색결과로만 리턴

//////

0409 화

1. 가계부 리스트 뜯어보기

2. db랑 입히기

ALTER TABLE USERLIST ALTER COLUMN userId RESTART WITH 201;

// 더미 넣었을때 ID값 재설정

// 유저 전용 가계부 생성시
	-> 모달 호출로 폼 집어넣고
	-> 작성한내용 (가계부이름, 메모)
	-> 두개 + 로그인중 userId DB로 전달
	-> mypage에 가계부가 없으면 '지금 만들어보세요' 출력 -> 생성유도

// 가계부 테이블 새로 작성

CREATE TABLE LEDGER (
  ledgerId INTEGER NOT NULL AUTO_INCREMENT,
  userId INTEGER NOT NULL,
  ledgerName VARCHAR(100) NOT NULL,
  ledgerRegDate DATETIME NOT NULL DEFAULT TO_CHAR(CURRENT_TIMESTAMP, 'yyyy-MM-dd HH:mm:ss'),
  ledgerModDate DATETIME NULL,
  ledgerMemo VARCHAR(200) NULL
);

ALTER TABLE `LEDGER` ADD CONSTRAINT `PK_LEDGER` PRIMARY KEY (
	`ledgerId`
);


INSERT INTO LEDGER (userId, ledgerName, ledgerMemo) values ('1','가계부테스트1','가계부메모1');

        
withCredentials: true

일반유저는 가계부 5개

프리미엄은 가계부 무한대

입출금 테이블 만들때

userId / ledgerId / 입 / 출 / 카테고리 / / / / /

userId : 세션에서 뜯어오면 되고

ledgerId : 

이번주내로

가계부 관련된건 다 끝내야대요

기본 입출작성 / 차트 / 캘린더보기  <--- 여기에 추가하는거

기본적으로 이렇게는 완성시키고

그다음에 고려할게 금융결제 / 통장 / 카드 데이터 삽입해서 (제일 오래걸릴거같음)

안되면 말고

굳이 공공데이터 쓰고싶으면 그냥 막

적당한거 찾아서 화면에 띄우는거 만들어도대요 저희프로젝트에 맞는걸로 <-- 면접볼때 좋으니까

///

일반유저 <-> admin Navbar 로직 전반적으로 수정할것

프로필 안나오는 이유 찾아보기


/// 0411 목

가계부리스트 -> 가계부 편집 -> 입출내역 표시 창으로

가져올 데이터
1. 수입
2. 지출
1. 수입 카테고리
2. 지출 카테고리

///

기간별 표시 기본으로

데이터 표시

가계부 아이디 / 카테고리 아이디(생활비,용돈,식비,문화,의료,경조사,기타) / 지출경로 (카드, 현금) / 지출금액 / 지출내용 / 시간 / 기타메모


//

1. 양쪽에 지출 / 수입 띄우기
2. 카테고리 편집 기능 + 지정한 카테고리로 보이게
3. 

// 갈아없자

로그인 - 바로 띄워주기 (마이페이지 x)

일자별로 리스트

메뉴 지출내역 / 수입내역 / 자료통계 / 자료차트 / 

날짜 / 총금액 (수입 + 지출 합계)

///

테이블 다시

1. 지출
지출일 / 지출금액 / 지출내역 / 카테고리 1 id / 카테고리 2 id / 결제수단(편집가능) / 메모

2. 수입
수입일 / 수입금액 / 수입내역 / 카테고리 1 id / 카테고리 2 id / 결제수단(편집가능) / 메모

3. 지출 카테고리 1 - 없음,식비,문화생활비,주거생활비,건강관리비,교통비,차량유지비,쇼핑비,미용비,교육비,사회생활비,유흥비,금융보험비,저축,기타

지출 카테고리 2 - 카테고리 1에 대응해서 달기 (숫자로 저장)

switch-case로 텍스트 표시

가계부 아이디 삭제


1. mypage 내용 수정 - 바로 가계부 띄우게

//// 0412 금 /////

6. 로그인후 처음 입장시 (일별 지출 총액) 페이지 보이기
1. 카테고리 최종 완성
2. 모달창 css꾸미기
3. 삭제 구현 //
3. 수정
4. 수입도 똑같이 만들기
5. 사이드바 / 윗메뉴에 항목별 선택 만들기
