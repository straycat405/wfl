<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.wfl.mbtest.question.QuestionMapper">
	<select id="getMyQuestion" parameterType="Integer" resultType="question">
		SELECT * FROM Question where userId = #{userId}
	</select>

	<delete id="deleteMyQuestion" parameterType="Integer">
		DELETE FROM QUESTION WHERE questionId = #{questionId}
	</delete>

	<insert id="postQuestion" parameterType="question">
		INSERT INTO QUESTION (questionTitle, questionContent, userId)
		VALUES (#{questionTitle}, #{questionContent}, #{userId})
	</insert>
	
	<insert id="setAnswer" parameterType="question">
		UPDATE QUESTION SET questionReply = #{questionReply} WHERE questionId = #{questionId}
	</insert>
	<select id="getQuestion" resultType="question">
		SELECT * FROM QUESTION WHERE questionId=#{questionId}
	</select>
	<update id="updateQuestion" parameterType="question">
		UPDATE QUESTION SET questionTitle = #{questionTitle}, questionContent = #{questionContent},
							questionRegDate = TO_CHAR(CURRENT_TIMESTAMP, 'YYYY-MM-dd HH:mm:ss')
				WHERE questionId = #{questionId}
	</update>
	<select id="getQuestionAll" resultType="question">
        SELECT * FROM QUESTION
	</select>
	
	
</mapper>